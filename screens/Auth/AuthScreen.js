import React, { useEffect, useState, useCallback } from "react";
import {
   View,
   Text,
   FlatList,
   Button,
   StyleSheet,
   ActivityIndicator,
   TouchableOpacity,
} from "react-native";

const AuthScreen = () => {
   return (
      <View>
         <Text>AuthScreen</Text>
      </View>
   );
};

export default AuthScreen;
